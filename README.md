# Freem Motion Portfolio

This repository contains Freem's motion design portfolio.

## Cloudflare Pages

Deploy the prebuilt site from `dist`.

Settings:

- Framework preset: None
- Build command: leave empty
- Build output directory: `dist`
- Root directory: repository root

Do not run `npm run build` for the current deployment, because the final optimized version is preserved in `dist`.
